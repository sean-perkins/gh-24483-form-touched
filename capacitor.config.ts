import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24483-mark-touched',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
